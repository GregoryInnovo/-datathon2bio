---
const { icon, title, description } = Astro.props;
---

<section class="flex flex-col w-full md:w-48 items-center">
  <img class="w-20 h-20" src={icon} alt={description} />
  <h2>{title}</h2>
  <a class="text-center font-bold text-sm">{description}</a>
</section>
